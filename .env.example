# Environment Configuration Template
# Copy this file to .env and fill in your actual values
# NEVER commit .env with real credentials to git

# Builder.io Configuration
# Get your API key from https://www.builder.io/c/docs/using-your-api-key
VITE_PUBLIC_BUILDER_KEY=your_builder_key_here

# Frontend URL for verification links
# Development: http://localhost:8080
# Production: https://yourdomain.com
FRONTEND_URL=http://localhost:8080

# Example API Configuration
PING_MESSAGE="ping pong"

# DATABASE CONFIGURATION
# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database
# Example: postgresql://myuser:mypassword@localhost:5432/mydatabase
DATABASE_URL="postgresql://username:password@localhost:5432/database_name"

# JWT Authentication
# Generate a secure random string for JWT signing
# You can generate one with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_secure_random_jwt_secret_here_minimum_32_characters
JWT_EXPIRES_IN=7d

# RESEND EMAIL SERVICE
# Sign up at https://resend.com to get your API key
# For testing, you can use "onboarding@resend.dev" as the from email
# For production, you need to verify your domain and use your domain email

RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
RESEND_FROM_EMAIL=onboarding@resend.dev

# Note: For production, replace RESEND_FROM_EMAIL with your verified domain email
# Example: RESEND_FROM_EMAIL=noreply@yourdomain.com

# Steps to get Resend API Key:
# 1. Sign up at https://resend.com
# 2. Go to API Keys section
# 3. Create a new API key
# 4. Copy and paste it above

# Admin email for contact form notifications
ADMIN_EMAIL=your-email@gmail.com

# CORS Configuration
# Comma-separated list of allowed origins for CORS
# Examples:
#   Development: http://localhost:5173,http://localhost:3000
#   Production: https://yourdomain.com,https://www.yourdomain.com
ALLOWED_ORIGINS=http://localhost:5173

# NODE ENVIRONMENT
NODE_ENV=development

# GOOGLE GEMINI AI CONFIGURATION
# Get your API key from: https://makersuite.google.com/app/apikey
# This is used for the AI chatbot functionality
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-1.5-flash

# SUPABASE CONFIGURATION
# Get these values from Supabase Dashboard: https://app.supabase.com
# 1. Go to your project dashboard
# 2. Click Settings > API
# 3. Copy the Project URL and anon/public API key

# Supabase Client Configuration (for frontend)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# IMPORTANT: For email verification to work, you MUST configure SMTP in Supabase Dashboard
# Steps to configure SMTP for magic link emails:
# 1. Go to Supabase Dashboard > Settings > Auth > SMTP Settings
# 2. Enable Custom SMTP and enter:
#    - Host: smtp.resend.com
#    - Port: 465 (or 587)
#    - Username: resend
#    - Password: Your RESEND_API_KEY (from above)
#    - Sender email: onboarding@resend.dev (testing) or noreply@yourdomain.com (production)
# See QUICK_FIX.md for detailed instructions

# Steps to enable Google OAuth:
# 1. Go to Supabase Dashboard > Authentication > Providers
# 2. Enable "Google" provider
# 3. Add your Google OAuth Client ID and Secret from Google Cloud Console
# 4. Add authorized redirect URI: https://your-project.supabase.co/auth/v1/callback

# STRIPE PAYMENT GATEWAY
# Get your keys from: https://dashboard.stripe.com/apikeys
# For testing, use test mode keys (pk_test_... and sk_test_...)
# For production, use live mode keys (pk_live_... and sk_live_...)
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
STRIPE_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Stripe Webhook Secret
# Get this after creating a webhook endpoint in Stripe Dashboard
# Dashboard > Developers > Webhooks > Add endpoint
# Endpoint URL (production): https://your-domain.com/api/stripe-webhook
# Select event: checkout.session.completed
# For local testing, use Stripe CLI: stripe listen --forward-to localhost:8080/api/stripe-webhook
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx


