# Environment Configuration Template
# Copy this file to .env and fill in your actual values
# NEVER commit .env with real credentials to git

# Builder.io Configuration
# Get your API key from https://www.builder.io/c/docs/using-your-api-key
VITE_PUBLIC_BUILDER_KEY=your_builder_key_here

# Example API Configuration
PING_MESSAGE="ping pong"

# DATABASE CONFIGURATION
# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database
# Example: postgresql://myuser:mypassword@localhost:5432/mydatabase
DATABASE_URL="postgresql://username:password@localhost:5432/database_name"

# JWT Authentication
# Generate a secure random string for JWT signing
# You can generate one with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_secure_random_jwt_secret_here_minimum_32_characters
JWT_EXPIRES_IN=7d

# EMAIL CONFIGURATION
# For development, set EMAIL_PROVIDER=console to log emails to console
# For production, set EMAIL_PROVIDER=smtp to send real emails
EMAIL_PROVIDER=console

# SMTP Configuration (required when EMAIL_PROVIDER=smtp)
# For Gmail SMTP:
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-gmail-app-password-here
SMTP_FROM=your-email@gmail.com

# For Gmail, you must use an App Password (not your regular password)
# Steps to generate Gmail App Password:
# 1. Enable 2-Step Verification: https://myaccount.google.com/security
# 2. Generate App Password: https://myaccount.google.com/apppasswords
# 3. Select "Mail" and "Other (Custom name)"
# 4. Copy the 16-character password (no spaces)
# 5. Paste it in SMTP_PASS above

# Admin email for contact form notifications
ADMIN_EMAIL=your-email@gmail.com

# CORS Configuration
# Comma-separated list of allowed origins for CORS
# Examples:
#   Development: http://localhost:5173,http://localhost:3000
#   Production: https://yourdomain.com,https://www.yourdomain.com
ALLOWED_ORIGINS=http://localhost:5173

# NODE ENVIRONMENT
NODE_ENV=development
