# Production Environment Configuration
# Copy this file to .env and fill in your actual values

# =====================================================
# DATABASE (PostgreSQL)
# =====================================================
# For Railway: Copy the Database URL from Railway dashboard
# For Supabase: Copy from Settings → Database → Connection Pooling
# For AWS RDS: Format: postgresql://user:password@host:5432/dbname

DATABASE_URL="postgresql://user:password@host:5432/database_name"

# =====================================================
# EMAIL (Gmail SMTP)
# =====================================================
# EMAIL_PROVIDER: "smtp" for production, "console" for development
EMAIL_PROVIDER=smtp

# Gmail SMTP Settings (do not change host/port)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Gmail Account
SMTP_USER=erayawellnesstravels@gmail.com

# IMPORTANT: Use Gmail App Password (not regular password)
# Generate at: https://myaccount.google.com/apppasswords
# Select "Mail" and "Windows Computer"
# Copy the 16-character password here
SMTP_PASS=xxxx_xxxx_xxxx_xxxx

# Sender Email (will appear as "From" in email)
SMTP_FROM=erayawellnesstravels@gmail.com

# =====================================================
# APPLICATION
# =====================================================
# Node environment
NODE_ENV=production

# Vite Public Builder Key (if using Builder.io)
VITE_PUBLIC_BUILDER_KEY=__YOUR_BUILDER_PUBLIC_KEY__

# Ping Message
PING_MESSAGE="pong"
